using UnityEngine;
using System.Collections;

/* Level manages the of the game on a level by level basis. 
 * 
 * 
 * 
 */

public class Level : MonoBehaviour {

    /*
    protected Player player;
    protected Spawn

	// Use this for initialization
	void Start () {
        player = GameObject.Find("Basic Player Controller A").GetComponent<Player>();


	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
    */

}
